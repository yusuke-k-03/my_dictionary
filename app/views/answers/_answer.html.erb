<% if current_user.liked_by?(answer.id) %>
   <td><%= link_to 'いいねを外す', destroy_like_path(answer), method: :DELETE, remote: true %> <%= answer.likes.count %> </td>
<% else %>
   <td><%= link_to 'いいね', create_like_path(answer), method: :POST, remote: true %> <%= answer.likes.count %></td>
<% end %>